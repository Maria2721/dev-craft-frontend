## 2026-03-03

**Что было сделано:** Починил падающий деплой, предложил API-контракт по регистрации и логину между фронтендом и бекендом, 
поразымышлял над первой фичей.

- **Деплой.** Workflow падал по разным причинам: сначала «error in libcrypto» при загрузке SSH-ключа.
Потом на сервере закончилось место при сборке образа — «no space left on device»; диск увеличили до 20 ГБ. 
Затем при деплое падал prisma migrate deploy: в финальном образе приложения не было папки prisma, 
в Dockerfile добавил копирование prisma из билдер стадии. После этого деплой прошёл успешно.
Понимание «что именно сломалось» пришло по логам в GitHub Actions и на сервере. Хорошо сработала проверка ключа через
ssh-keygen и явное сообщение об ошибке - так быстрее было сузить причину проблемы.
- **API-контракт** Предложил описание контракта по регистрации и логину: POST /auth/register, POST /auth/login, 
единый формат ответа , коды ошибок 400/401/403/409.
- **Первая фича.** Думал над AI-помощником (чат с ИИ для подсказок и разбора ошибок) с сохранением истории диалога в базу данных
для создания контекста (т.н. конверсейшион айди) для авторизованных.

**Затраченное время:** около 4–5 часов.
